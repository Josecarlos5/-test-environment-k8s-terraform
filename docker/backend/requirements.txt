# Core Flask framework
Flask==3.0.0

# PostgreSQL database adapter for Python
psycopg2-binary==2.9.9

# Environment variable management (optional but recommended)
python-dotenv==1.0.0

# CORS support for cross-origin requests (useful for frontend-backend communication)
Flask-CORS==4.0.0

# Database migrations (optional but recommended for real projects)
Flask-Migrate==4.0.5

# SQLAlchemy ORM (optional, for more complex database operations)
Flask-SQLAlchemy==3.1.1

# HTTP client for making requests to external services (useful for testing integrations)
requests==2.31.0

# JSON Web Token support (if you need authentication)
PyJWT==2.8.0

# Testing framework (for unit tests)
pytest==7.4.3
pytest-flask==1.3.0

# Code formatting and linting (development dependencies)
black==23.11.0
flake8==6.1.0

# Application monitoring and logging
gunicorn==21.2.0  # WSGI HTTP Server for production deployment
